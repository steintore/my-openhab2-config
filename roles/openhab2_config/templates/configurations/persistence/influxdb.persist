Strategies {
    everyMinute : "0 * * * * ?"
}

Items {
    // persist all items once a day and on every change and restore them from the db at startup
    gTemperature, gPower, Power, gHumidity, Smappee, gWeather, System, gSpeedtest, gHeatingFloor : strategy = everyChange, everyMinute
}